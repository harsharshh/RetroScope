# PostgreSQL connection string pointing to the dedicated RetroScope database/schema.
# Update USER, PASSWORD, HOST, and PORT for your on-prem instance. If your Postgres
# database name contains hyphens (e.g. retro-scope-database) ensure it was created with
# double quotes and mirror that name here.
PRISMA_DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/retro-scope-database?schema=retro-scope-database"
DIRECT_DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/retro-scope-database?schema=retro-scope-database"
DATABASE_URL="${PRISMA_DATABASE_URL}"
